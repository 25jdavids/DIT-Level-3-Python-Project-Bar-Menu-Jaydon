import tkinter as tk
from tkinter import messagebox

#Class
class Order:
    def __init__(self, name, age, drink, size, price):
        self.name = name
        self.age = age
        self.drink = drink
        self.size = size
        self.price = price

#The menu system displayed in a list
prices = {
    "Coke": {"Small": 3, "Medium": 4, "Large": 5},
    "Juice": {"Small": 4, "Medium": 5, "Large": 6},
    "Water": {"Small": 2, "Medium": 3, "Large": 4},
    "Beer": {"Small": 6, "Medium": 8, "Large": 10},
    "Wine": {"Small": 7, "Medium": 9, "Large": 11},
    "Vodka": {"Small": 8, "Medium": 10, "Large": 12},
    "Whiskey": {"Small": 9, "Medium": 11, "Large": 13},
    "Rum": {"Small": 8, "Medium": 10, "Large": 12},
    "Gin": {"Small": 8, "Medium": 10, "Large": 12},
    "Margarita": {"Small": 10, "Medium": 12, "Large": 14},
    "Mojito": {"Small": 10, "Medium": 12, "Large": 14},
    "Piña Colada": {"Small": 11, "Medium": 13, "Large": 15},
    "Espresso Martini": {"Small": 12, "Medium": 14, "Large": 16}}

#Input validation to check if the useer is entering the right inputs and text into the messageboxes
def place_order():
    name = name_entry.get()
    age = age_entry.get()
    drink = drink_var.get()
    size = size_var.get()
    
 #Checking for blank messageboxes   
    if name == "" or age == "":
        messagebox.showerror("Error, Please fill in all of the given fields")
        return

    try:
        age = int(age)
    except:                  #Checkinf if the information entered is a number or not
        messagebox.showerror("Error, The age that you have entered needs to be a number")
        return

    if age < 18:            #Checking if the age entered is valid to place and order
        messagebox.showerror("Error, You must be over the age of 18 to place an order here")
        return

        price = prices[Drink][Size]

        order = Order(name, age, drink, size, price)
        order.save_to_file()
        
        messagebox.showinfo("Order sucessfully pushed through to staff", f"Order Placed!\nTotal: ${price}")

#Beginning of forming the GUI
        root = Tk.tk()
        root.title("Menu system for placing drinks orders")

        tk.label(root, text="Name").pack
        name_entry = tk.entry(root)
        name_entry.pack()

        tk.label(root, text="Age").pack
        age_entry = tk.entry(root)
        age_entry.pack()

        drink_var = tk.StringVar(value="Coke")
        tk.Label(root, text="Drink").pack()
        tk.OptionMenu(root, drink_var, "Coke", "Juice", "Water").pack()

        





